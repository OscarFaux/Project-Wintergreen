# dmm map merger hook
# needs additional setup, see tools/mapmerge/install.txt
# *.dmm merge=merge-dmm

# Nope, now they're binaries.
*.dmm binary
# dmi icon merger hook
# needs additional setup, see tools/dmitool/merging.txt
*.dmi merge=merge-dmi
# force changelog merging to use union
html/changelog.html merge=union
*.rsc filter=lfs diff=lfs merge=lfs -text
*.dmb filter=lfs diff=lfs merge=lfs -text
*.dmi filter=lfs diff=lfs merge=lfs -text
*.ogg filter=lfs diff=lfs merge=lfs -text
*.png filter=lfs diff=lfs merge=lfs -text
